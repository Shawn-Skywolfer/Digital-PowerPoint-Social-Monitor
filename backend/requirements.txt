# ---- Web 框架 ----
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# ---- 数据 ----
sqlalchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.3

# ---- 安全 ----
PyJWT>=2.8
bcrypt>=4.1
cryptography>=42

# ---- HTTP / 外部调用 ----
httpx>=0.27

# ---- 任务调度 ----
apscheduler>=3.10,<4.0

# ---- 导出 ----
openpyxl>=3.1

# ---- MCP 客户端(可选, 用于连接 FireCrawl 等 MCP server) ----
mcp>=1.2

# ---- 测试 ----
pytest>=8.0
pytest-asyncio>=0.23
